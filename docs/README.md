# docs

These pages are the repository's operational documentation and are the intended
content for the GitHub wiki.

At the time of writing the wiki remote for this repository does not exist yet —
`git@github.com:gaulatti/croccante.wiki.git` is not reachable, because a GitHub
wiki is only created once its first page is added through the web UI. The
startup updater reports this as `wiki-unavailable`.

Once the wiki has been initialised, move these pages there and leave pointers
behind:

| File | Wiki page |
|------|-----------|
| `architecture.md` | Architecture |
| `operations.md`   | Operations |
